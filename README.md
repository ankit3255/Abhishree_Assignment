# Abhishree_Assignment

#Installation

    Clone the repository to your local machine.

    Install the required packages using pip:

pip install beautifulsoup4 requests

Run the a.py script to start the web scraper.